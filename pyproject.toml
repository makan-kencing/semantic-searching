[project]
name = "semantic-searching"
version = "0.1.0"
description = "Add your description here"
requires-python = ">=3.13"
dependencies = [
    "fastapi[standard]",
    "polars",
    "txtai",
    "gensim",
    "scikit-learn"
]

[dependency-groups]
build = [
    "click",
    "kagglehub[polars-datasets]",
    "aiohttp[speedups]",
    "beautifulsoup4",
    "lxml",
    "tqdm"
]
dev = [
    "ipykernel>=7.2.0",
]

[project.scripts]
web = "semantic_searching.app:app"
