[project]
name = "semantic-searching"
version = "0.1.0"
description = "Add your description here"
requires-python = ">=3.14"
dependencies = [
    "fastapi[standard]",
    "click",
    "polars",
]

[dependency-groups]
build = [
    "kagglehub[polars-datasets]",
    "aiohttp[speedups]",
    "beautifulsoup4",
    "lxml",
    "tqdm"
]
dev = [
    "ipykernel>=7.2.0",
]
